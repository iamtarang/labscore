<div id="layoutSidenav_nav">
    <nav class="sb-sidenav accordion sb-sidenav-dark" id="sidenavAccordion">
        <div class="sb-sidenav-menu">
            <div class="nav">
                <div class="sb-sidenav-menu-heading"></div>
                <a class="nav-link <% if (path.indexOf('/dashboard') != '-1') { %> active <% } %>" href="/dashboard">
                    <div class="sb-nav-link-icon"><i class="fas fa-tachometer-alt"></i></div>
                    Dashboard
                </a>

                <% if (loggedin_user.role==1) { %>

                    <a class="nav-link <% if (path.indexOf('/courses') != '-1') { %> active <% } %>" href="/courses">
                        <div class="sb-nav-link-icon"><i class="fas fa-list"></i></div>
                        Courses
                    </a>
                    <!-- <a class="nav-link <% if (path.indexOf('/assignment_data') != '-1') { %> active <% } %>"
                        href="/assignment_data">
                        <div class="sb-nav-link-icon"><i class="fas fa-list"></i></div>
                        Assignments Data
                    </a> -->
                    <a class="nav-link <% if (path.indexOf('/users_data') != '-1') { %> active <% } %>" href="/users">
                        <div class="sb-nav-link-icon"><i class="fas fa-list"></i></div>
                        Users Data
                    </a>

                    <% } %>


                        <% if (loggedin_user.role==2) { %>
                            <a class="nav-link <% if (path.indexOf('/assignments') != '-1') { %> active <% } %>"
                                href="/assignments">
                                <div class="sb-nav-link-icon"><i class="fas fa-list"></i></div>
                                Assignments
                            </a>
                            <a class="nav-link <% if (path.indexOf('/assignments/createAssignment') != '-1') { %> active <% } %>"
                                href="/assignments/createAssignment">
                                <div class="sb-nav-link-icon"><i class="fas fa-file"></i></div>
                                Create Assignment
                            </a>
                            <a class="nav-link <% if (path.indexOf('/studentSubmissions') != '-1') { %> active <% } %>" href="submitted">
                                <div class="sb-nav-link-icon"><i class="fas fa-list"></i></div>
                                Student Submissions
                            </a>
                            <a class="nav-link <% if (path.indexOf('/grade') != '-1') { %> active <% } %>" href="/">
                                <div class="sb-nav-link-icon"><i class="fas fa-check"></i></div>
                                Grade Assignments
                            </a>
                            <% } %>
                            
                        <% if (loggedin_user.role==3) { %>
                            <a class="nav-link <% if (path.indexOf('/') != '-1') { %> active <% } %>"
                                href="studentAssignments">
                                <div class="sb-nav-link-icon"><i class="fas fa-list"></i></div>
                                Current Assignments
                            </a>
                            <a class="nav-link <% if (path.indexOf('/') != '-1') { %> active <% } %>"
                                href="studentAssignments/submittedAssignment">
                                <div class="sb-nav-link-icon"><i class="fas fa-list"></i></div>
                                Submitted Assignments
                            </a>
                            <% } %>
            </div>
        </div>
        <div class="sb-sidenav-footer">
            <div class="small">Logged in as: <%= loggedin_user.username %>
            </div>
        </div>
    </nav>
</div>